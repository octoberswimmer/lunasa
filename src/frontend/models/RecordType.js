/* @flow strict */

export type RecordTypeInfo = {
	active: boolean,
	available: boolean,
	defaultRecordTypeMapping: boolean,
	developerName: string,
	master: boolean,
	name: string,
	recordTypeId: string,
	urls: {
		layout: string
	}
}
